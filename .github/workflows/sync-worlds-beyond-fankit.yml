name: Sync Worlds Beyond Fan Kit

on:
  push:
    branches: [main]
    paths:
      - scripts/sync-worlds-beyond-fankit.mjs
      - .github/workflows/sync-worlds-beyond-fankit.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Archive official Worlds Beyond Fan Kit assets
        run: node scripts/sync-worlds-beyond-fankit.mjs
      - name: Commit archived assets
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A assets/fankits/worlds-beyond
          if git diff --cached --quiet; then
            echo "Worlds Beyond Fan Kit archive unchanged."
            exit 0
          fi
          git commit -m "chore: archive Worlds Beyond Fan Kit assets"
          git push
