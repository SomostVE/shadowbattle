name: Apply 0.5.27 target exact copy

on:
  push:
    branches:
      - engine-v6-0.5.27-target-copy-order

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: engine-v6-0.5.27-target-copy-order
          fetch-depth: 0
      - run: echo runner-ok
